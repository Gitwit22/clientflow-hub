# VITE_* values are embedded in browser assets and must never contain secrets.
# The production build defaults to https://clientflow-vjqd.onrender.com.
VITE_API_URL=http://localhost:3000

# Server-only database and n8n settings belong in nxt-lvl-api2/.env.local.